services:
  todoist-mcp:
    image: ghcr.io/bantznet/todoist-mcp:latest
    container_name: todoist-mcp
    restart: unless-stopped

    environment:
      # Required. Comes from .env
      TODOIST_API_KEY: ${TODOIST_API_KEY}

    ports:
      - "3000:3000"